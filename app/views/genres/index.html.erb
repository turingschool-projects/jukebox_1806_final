<% @genres.each do |genre| %>
  <%= genre.name %>
<% end %>

<% if current_admin? %>
  <h1>Create new genre:</h1>
    <%= form_for @genre do |f| %>
      <%= f.label :name %>:
      <%= f.text_field :name %>
      <%= f.submit %>
    <% end %>
<% end %>
