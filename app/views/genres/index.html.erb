<h1>All Genres</h1>

<% @genres.each do |genre| %>
  <ul>
    <li><%= link_to genre.name, genre_path(genre) %></li>
  </ul>
<% end %>

<% if current_admin? %>
  <%= link_to "Add New Genre", new_admin_genre_path, class: 'btn' %>
<% end %>
